It allows two system to establish a peer-to-peer connection and share their media streams. 
Technologies used: Socket io (signaling), WebRTC (p2p connection), React JS(client), Node JS, Express 
JS (Server), MongoDB (Database). 